{
  "name": "@bitwarden/autofill-playwright",
  "version": "0.0.0",
  "description": "Bitwarden Autofill Playwright Tests",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bitwarden/autofill-playwright.git"
  },
  "author": "Bitwarden Inc. <hello@bitwarden.com> (https://bitwarden.com)",
  "license": "SEE LICENSE IN LICENSE.txt",
  "bugs": {
    "url": "https://github.com/bitwarden/autofill-playwright/issues"
  },
  "homepage": "https://bitwarden.com",
  "devDependencies": {
    "@playwright/test": "1.37.0",
    "@types/chrome": "0.0.243",
    "@types/node": "20.5.0",
    "cross-env": "7.0.3",
    "cross-fetch": "4.0.0",
    "dotenv": "16.3.1",
    "husky": "8.0.3",
    "lint-staged": "14.0.0",
    "prettier": "3.0.2",
    "rimraf": "5.0.1",
    "ts-node": "10.9.1",
    "tslib": "2.6.1",
    "typescript": "5.1.6"
  },
  "lint-staged": {
    "*": "prettier --cache --write --ignore-unknown"
  },
  "prettier": {
    "trailingComma": "es5"
  },
  "scripts": {
    "prepare": "husky install",
    "pretest": "rimraf tests-out screenshots test-summary test-results && tsc --incremental -p tests/tsconfig.json",
    "build:extension": "cd clients/apps/browser && rimraf build && npm run build",
    "build:extension:autofill": "cd clients/apps/browser && rimraf build && cross-env AUTOFILL_VERSION=2 npm run build",
    "build:extension:autofill:mv3": "cd clients/apps/browser && rimraf build && cross-env AUTOFILL_VERSION=2 MANIFEST_VERSION=3 npm run build",
    "build:extension:mv3": "cd clients/apps/browser && rimraf build && npm run build:mv3",
    "build:extension:prod": "cd clients/apps/browser && rimraf build && npm run build:prod",
    "seed:vault:account": "./scripts/create-account.sh",
    "seed:vault:ciphers": "./scripts/cli-serve-helper.sh && ts-node ./scripts/vault-seeder/vault-seeder.ts && kill $(ps -e | grep 'bw serve' | grep -v 'vault-seeder' | awk '{print $1}')",
    "seed:vault:ciphers:refresh": "./scripts/cli-serve-helper.sh && REFRESH=true ts-node ./scripts/vault-seeder/vault-seeder.ts && kill $(ps -e | grep 'bw serve' | grep -v 'vault-seeder' | awk '{print $1}')",
    "setup:extension": "rimraf clients && git clone https://github.com/bitwarden/clients.git clients && cd clients && npm ci",
    "setup:ssl": "./scripts/generate-certs.sh",
    "setup:vault": "npm run seed:vault:account && npm run seed:vault:ciphers",
    "test:all": "npm run pretest && playwright test",
    "test:all:debug": "npm run pretest && playwright test --debug",
    "test:all:headless": "npm run pretest && HEADLESS=true playwright test",
    "test:public": "npm run pretest && TARGET=public playwright test",
    "test:public:debug": "npm run pretest && TARGET=public playwright test --debug",
    "test:public:headless": "npm run pretest && TARGET=public HEADLESS=true playwright test",
    "test:static": "npm run pretest && TARGET=static playwright test",
    "test:static:debug": "npm run pretest && TARGET=static playwright test --debug",
    "test:static:headless": "npm run pretest && TARGET=static HEADLESS=true playwright test",
    "test:webserve": "cd clients/apps/web && npm run build:bit:watch",
    "test:webserve:bitwarden": "cd clients/apps/web && ENV=cloud npm run build:oss:watch"
  }
}
